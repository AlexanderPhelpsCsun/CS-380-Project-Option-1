/**
simple aircraft class that just stores an aircraft id so the airports and flights can identify them
@author Alexander
@version 1.0
*/
public class Aircraft {
    String id;

/**
*Constructor makes an aircraft and assings it an id
*
*@param String id
*/
    public Aircraft(String id)
    {
        id = id;
    }
}
